#Replace the below place holders
server:
  #address:
  port: 8443
  sessionTimeout: 30

spring:
  http:
    multipart:
      max-file-size: 15MB
      max-request-size: 16MB
digsig:
  instance: 
  hostnameVerification:
  wdslURL: https://twod-test.trustweaver.com/ts/svs.asmx?wsdl
  trustweaver:
    keystore: src/test/resources/tw_keystore_stag_2021.pfx
    keystoreType: PKCS12
    keystorePassword: datacert
    truststore: src/test/resources/tw_truststore_stg.jks
    truststoreType: JKS
    truststorePassword: datacert
management:
  endpoints:
    web:
      exposure:
        include: "*"